# Project-owned research flow

**Status:** Implemented and locally verified; publication tracked in project memory.

The running production server predates harness support while serving a newly rebuilt browser bundle. Add a shared API compatibility version and reject incompatible mutations with a clear restart instruction. Verify the real run endpoint accepts adaptive options, then refresh the identified idle local server after the final build.

Make Harness configuration/reference only: persisted research defaults, guidance, tool access, topology, and runtime information. Move per-run limits, run history, evidence, reports, clarification, steering, and stop into project chats. Preserve legacy run inspection. Keep one launch surface: the chat composer with Chat/Research selection. Validate the complete project flow and narrow layouts, then commit and publish coherent checkpoints.
